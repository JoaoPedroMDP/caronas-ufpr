import { StyleSheet } from "react-native";


const gs = StyleSheet.create({
    flexRow: {
        display: "flex",
        flexDirection: "row",
    },
    flexCol: {
      display: "flex",
      flexDirection: "col",
    },
    justifyCenter: {
      justifyContent: "center",
    },
})

export default gs;